//
function create() {
	if (SONG.meta.displayName.toLowerCase() = "stargazer" || SONG.meta.displayName.toLowerCase() = "singularity") { // add more songs to night tint here
    boyfriend.setColorTransform(0.5, 0.5, 0.8);
	dad.setColorTransform(0.5, 0.5, 0.8);
	gf.setColorTransform(0.5, 0.5, 0.8);
	}
}